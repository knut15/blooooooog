# setup-local.sh 가 이 형식으로 .env 를 자동 생성한다.
# 실제 값이 든 .env 는 커밋하지 않는다.
DB_NAME=wp_blog
DB_USER=wp_user
DB_PASS=<자동 생성됨>
WP_ADMIN_USER=<원하는 아이디>
WP_ADMIN_PASS=<자동 생성됨>
WP_ADMIN_EMAIL=<이메일>
WP_URL=http://localhost:8080
WP_TITLE=CurvezLog
