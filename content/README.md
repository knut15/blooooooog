# content

발행한 글의 원본 마크다운.

원본은 Obsidian 볼트(`~/Documents/knowledge`)에 있고 여기는 저장소용 사본이다.
워드프레스에는 `blog` 스킬의 `publish.sh` 가 이 마크다운을 HTML 로 변환해 올린다.

```
content/posts/<제목>.md
```

프론트매터는 볼트 규약을 그대로 따른다 — `title`·`created`·`tags`·`category`.
